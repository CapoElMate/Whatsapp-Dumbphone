version: '3.8'

services:
  whatsapp-gateway:
    build: .
    container_name: whatsapp-gateway
    restart: unless-stopped
    ports:
      - "40001:40001"
    volumes:
      - ./session:/app/.wwebjs_auth
    env_file:
      - .env